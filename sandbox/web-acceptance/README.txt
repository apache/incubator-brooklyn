Currently requires manual execution using Selenium1 group.

Was passing previously but not tried in a while.

When can run as part of Integration group we should move to usage/
and run as part of maven full/integration build.

