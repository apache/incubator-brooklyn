package brooklyn.entity.cluster

import brooklyn.entity.group.Tier
import brooklyn.entity.trait.Startable

/**
 * Represents a controller mechanism for a {@link Cluster}.
 */
public abstract class Controller extends Tier implements Startable {
    
}