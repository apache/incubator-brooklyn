package brooklyn.entity.database;

import org.testng.annotations.Test;

public class PlaceholderTest {
    
    // TODO remove this when we have real tests (but build fails if there are no tests) 
    @Test
    public void noop() {}
    
}
